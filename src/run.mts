import { addition, division, multiplication, subtraction } from "./math.mjs"

// division(2);
// division(9, 3, true);
// subtraction(2);
// multiplication(8);
// export function addition(addend: number, sheetCount = 1, includeAnswer = false, min = 0, max = 12, rows = 10, cols = 10) {
addition(10, 3, false, 0, 10, 10, 10, true)